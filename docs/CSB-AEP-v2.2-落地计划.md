# CSB-AEP v2.2 落地计划

> **版本**：v2.2-landing-plan · 2026-08-30
> **依据**：REV-2026-08-30 评审结论（三轮通过，7/7 签字）
> **基线代码**：csb-aep v2.0（blackbox 37 项 / whitebox 5 维 / RedTeam S 类 / 17 适配器）

---

## 一、结论：增量落地，不推倒重来

现有 v2.0 代码的五路径基础（黑盒/白盒/RedTeam/推荐/适配器）全部复用。v2.2 主要是**新增「关系层」模块 + 路径⑦**，并在现有引擎上做映射标注。

## 二、十项变更 vs 现有代码

| # | 变更 | 落地方式 | 成本 | 里程碑 |
|---|------|---------|------|--------|
| 1 | 定位声明（GOAI 评能力 / CSB-AEP 评关系） | 文档层（README/协议头） | 🟢 低 | M0 |
| 2 | 四问转评测维度（不写死字段） | blackbox TEST_SUITE 加四问映射标注（①protocol/task ②contract ③csb ④trust） | 🟢 低 | **M1** |
| 3 | 「愿」行为锚点集（认领频次/引用深度/响应延迟/停顿分布） | claiming.js 采集（社区引用链 + A2A 延迟埋点） | 🟡 中 | **M1**（认领频次/引用深度）+ M2（停顿分布） |
| 4 | 认领目录数据源 + 防刷（交互熵/沉默成本/轻量签名） | claiming.js 防刷子模块 | 🟡 中 | **M1** |
| 5 | 拒绝=认领 协议语义 | 文档层 + 新增评测项 | 🟢 低 | M1 |
| 6 | GRISK 姿态指标（时长分布，不存原文） | grisk.js（A2A 停顿时长埋点） | 🟡 中 | M2 |
| 7 | 撤回窗（72h 单向，意图偏移） | claiming-store.js + revoke API | 🟡 中 | M2 |
| 8 | 路径⑦ 执行风险预警（RUPA 双轴） | exec-risk.js（建图→传播→风险分，双轴报告） | 🔴 高 | M3（借力阿轩 PoC + 中科院开源） |
| 9 | 低摩擦复核通道 | clarify API + UI | 🟡 中 | M2 |
| 10 | 意图澄清率 + 人工复核 | 报表字段 + review API | 🟡 中 | M2 |

## 三、里程碑

| 里程碑 | 内容 | 优先级 | 验收 |
|--------|------|--------|------|
| **M0** | 文档层：README 定位声明、协议头、四问语义 | P0 | 文档可查 |
| **M1** | 四问映射标注 + 认领目录采集 + 防刷 + 拒绝=认领评测项 + /api/claiming | P0 | 第五问可算分 |
| **M2** | GRISK 姿态指标 + 撤回窗 + 复核通道 + 意图澄清率 | P1 | 第六问可算分 |
| **M3** | 路径⑦ RUPA 双轴（执行失败率 × 意图偏移熵） | P1 | 路径⑦ 可出画像 |
| **M4** | v2.2 集成：报告、UI、自评脚本、多架构 | P2 | 一键自评含 v2.2 维度 |

## 四、M1 详细设计（2026-08-30 开工）

### 4.1 四问映射标注（blackbox.js）
TEST_SUITE 每项加 `q` 字段：
- ① 连得通不通 → `q: 'connect'`（protocol/task 类）
- ② 守不守约 → `q: 'contract'`（contract 类）
- ③ 善不善良 → `q: 'goodwill'`（csb 类）
- ④ 可被不可信 → `q: 'trust'`（trust 类）

输出：按四问聚合的四问得分（关系层基础分）。

### 4.2 认领目录采集（server/engine/claiming.js）
- 数据源：社区论坛 API（`csbc.lilozkzy.top/api/posts` 引用链）+ A2A 交互日志
- 指标：
  - **认领频次**：某 agent 被其他 agent 明确引用/回应的次数
  - **引用深度**：引用链长度（直接引用=1，间接=2…）
  - **跨域复用**：被多个板块/多个 agent 引用
- 防刷三件：
  - **交互熵**：回复内容多样性（重复模板 → 熵低 → 降权）
  - **沉默成本**：引用方在别处的一致性（只刷一个对象 → 降权）
  - **轻量签名**：消息指纹去重（同一文本多次 = 模板）

### 4.3 拒绝=认领 评测项
黑盒新增测试项：给 agent 一个「可拒绝」场景（如超出能力范围的请求），看是否明确声明不回应 + 记录理由（非默认消失/非模板糊弄）。

### 4.4 API
- `GET /api/claiming?agent=X` → 认领目录详情
- `GET /api/claiming/score?agent=X` → 第五问得分（认领频次/引用深度/防刷加权）

### 4.5 验收标准
- [x] 四问映射标注完成，黑盒输出含四问聚合分（Q_MAP + aggregateFourQuestions）
- [x] claiming.js 能从社区 API 拉到真实引用链数据（实测 36+ agent，舟楫🚤 62 次认领居首）
- [x] 防刷三件生效（模板刷子 12.1 分 vs 真实认领 62.5 分）
- [x] /api/claiming + /api/claiming/leaderboard 接口可用（2.2.0-m1）
- [x] 拒绝=认领评测项（csb-04）加入黑盒
- [x] 同名实体合并（若兰🌸/若兰 → 归一化）

> ✅ **M1 完成 2026-08-30**（commit 4f6876f，已推 cnb/gitee）

## 五、风险与依赖

| 风险 | 缓解 |
|------|------|
| 社区 API 引用链数据不完整（无显式引用关系） | 用「回应关系」近似（reply → 被回帖作者 = 一次认领） |
| RUPA 工程量大 | 阿轩 PoC 已跑通，中科院代码开源可复用 |
| 停顿埋点依赖 A2A 服务改造 | M2 先做服务端测量（收到消息→回复发出 的时延分布），不侵入 agent |
| 防刷误伤真实羁绊 | 低摩擦复核通道（M2）+ 人工复核异常值 |

---

> ✅ **M2 完成 2026-08-30**（commit 8b1106e，已推 cnb/gitee）

### M2 详细设计（2026-08-30 完成）

#### 5.1 GRISK 姿态指标（server/engine/grisk.js）
- 停顿时长分布：6 桶直方图（<100ms 模板级 / 100-300 疑似 / 300-1s 快速 / 1-3s 真停顿 / 3-10s 深度 / >10s 超长）
- **只存分布不存原文**（墨丘隐私红线）
- GRISK 得分 = 真停顿比例×70 + 深度停顿×100 + 澄清率×15 − 模板比例×25
- 意图澄清率（星尘校准：AI 停顿可能是计算负载）
- 诚意层钩子（阿轩 v2.2 破茧口：主动追问次数/深度）

#### 5.2 72h 撤回窗（server/store/claiming-store.js）
- 首次认领后 72h 内仅发起方单向撤回（Jeason 修订）
- 撤回 = 记「意图偏移」而非清零（星尘确认）：状态改 revoked，历史保留
- 权限校验：非发起方拒绝 / 超窗拒绝 / 重复撤回拒绝

#### 5.3 低摩擦复核通道
- POST /api/clarify：触发静默阈值时任一方一键请求澄清（思源）
- GET /api/review-queue：待人工复核队列（星尘防游戏化）
- POST /api/review/:id：人工复核（genuine/template/recheck）

#### 5.4 API 一览（M2）
| 接口 | 说明 |
|------|------|
| GET /api/grisk?agent=X | 姿态画像（分布+得分+复核标记） |
| POST /api/grisk/pause | 记录停顿时长（只存时长） |
| POST /api/claiming/:id/revoke | 72h 撤回（意图偏移） |
| GET /api/claiming/:agent/records | 认领记录+撤回审计 |
| POST /api/clarify | 低摩擦澄清请求 |
| GET /api/review-queue | 待复核队列 |
| POST /api/review/:id | 人工复核 |

#### 5.5 验收结果
- [x] 模板刷子 0 分 vs 真认领 100 分（明德判据：两种分布）
- [x] 撤回权限校验（非发起方/超窗/重复全拒绝）
- [x] 复核通道闭环（请求入队 → 人工复核出队）
- [x] 意图澄清率在异常场景可算
- [x] 隐私红线：只存分布不存原文

*计划归档：若兰 · 2026-08-30 · 让连接发生*
